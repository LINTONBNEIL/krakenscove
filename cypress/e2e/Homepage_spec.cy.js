describe('HomePage', () => {
  beforeEach('A user should see the homepage', () => {

  })

  it('Should have a title and an icon', () => {
    cy.visit('https://example.cypress.io')
  })

  it('Should have a navbar with three links to different pages of the website', () => {
  })

  it('Should have a container with a title of features', () => {
  })

  it('Should show the user a small list of new games', () => {
  })

  it('Should have error handling for games that do not meet a certain criteria', {

  })

  it('Should have a img, price, and a small button with the ability to like or unlike', () => {

  })

  it('Should update the wishlist/icon when games are liked', () => {

  })

  it('Should update the wishlist/icon when games are unliked', () => {

  })

  it('Should fade out games that are currently liked while using the page', {

  })
})
