describe('Wishlist', () => {
  beforeEach('A user should see the Wishlist page', => {

  })

  it('Should show the title and the icon', () => {

    cy.visit('https://example.cypress.io')
  })

  it('Should display the navbar with three buttons which link to a new page', () => {

  })

  it('Should show whether or not the wishlist button/icon is updated', () => {

  })

  it('Should show a container with possible items added to the wishlist', () => {

  })

  it('Should have error handling when wishlist container is empty', () => {

  })

  it('Should show the img, title, store, and price on each item', () => {

  })

  it('Should have links to where to buy these certain games', () => {

  })

  it('Should give the user the ability to unlike certain games', () => {
    
  })

  it('Should calculate the total price for all wishlisted items', () => {

  })

  it('Should have a starting total of zero or nothing', () => {

  })

  it('Should have a clear button for all items on the wishlist page', () => {

  })
})
